import styled from 'styled-components';

export const BackgroundStyles = styled('div')`

  display: flex;

  .circle {
    border: 1px solid black;
    flex: 1;
    aspect-ratio: 1 / 1;
    border-radius: 50%;
  }


`